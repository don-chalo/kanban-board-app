## 1. Board comments busy state

- [x] 1.1 Show busy state on board comment create, edit, and remove in `BoardDetailPage.tsx` (disabled entry plus inline spinner with no duplicate requests, row spinner with disabled actions, dialog YES disabled with spinner, restore on failure) and verify `BoardDetailPage.test.tsx` covers busy on all three mutations plus restore on failure

## 2. Verification

- [x] 2.1 Run `npm test`, `npm run lint`, `npm run build` in `kanban-front-end` and verify all pass
